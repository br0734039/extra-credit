#include <iostream>
#include <cstdint>
#include <cmath>

int main()
{
	int32_t s_squares = 0, s_sum = 0;

	for (int i = 1; i <= 100; ++i)
	{
			s_squares += pow(i, 2);
			s_sum += i;
	}
	std::cout << (pow(s_sum, 2) - s_squares) << std::endl;
	return 0;
}
